name: qr_scanner_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.3

  flutter:
    sdk: flutter
  qr_scanner:
    path: ../


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

